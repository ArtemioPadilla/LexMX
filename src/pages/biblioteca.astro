---
import BaseLayout from '../components/layout/BaseLayout.astro';
import LibraryIsland from '../islands/LibraryIsland.tsx';
import { getTranslation } from '../i18n/utils';
const t = getTranslation(Astro.url);
---

<BaseLayout title={`${t('library.title')} - LexMX`} description={t('library.subtitle')}>
  <main class="min-h-screen bg-white dark:bg-gray-900">
    <div class="mx-auto max-w-5xl px-4 py-12 sm:px-6">
      <h1 class="text-3xl font-bold text-gray-900 dark:text-white" data-i18n="library.title">{t('library.title')}</h1>
      <p class="mt-2 mb-8 text-gray-600 dark:text-gray-300" data-i18n="library.subtitle">{t('library.subtitle')}</p>
      <LibraryIsland client:only="react" />
    </div>
  </main>
</BaseLayout>
